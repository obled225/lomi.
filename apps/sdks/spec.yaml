openapi: 3.0.3
info:
  title: lomi. API
  version: 1.1.0
  description: Payment processing API for francophone West African businesses.
  contact:
    name: lomi. Support
    email: hello@lomi.africa
    url: https://lomi.africa
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
servers:
  - url: https://api.lomi.africa/v1
    description: Production server
  - url: https://sandbox.api.lomi.africa/v1
    description: Sandbox server
security:
  - ApiKeyAuth: []
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-KEY
      description: API key for authentication. Get yours from the lomi. dashboard.
  schemas:
    beneficiary_payouts:
      type: object
      description: beneficiary payouts resource object
      properties:
        account_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_method_code:
          type: string
        payout_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    beneficiary_payouts_create:
      type: object
      description: Request body for creating a beneficiary payouts object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        account_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        payment_method_code:
          type: string
        payout_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
    beneficiary_payouts_update:
      type: object
      description: Request body for updating a beneficiary payouts object. Only include fields you want to modify.
      properties:
        account_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        payment_method_code:
          type: string
        payout_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
    checkout_sessions:
      type: object
      description: checkout sessions resource object
      properties:
        allow_coupon_code:
          type: boolean
        allow_quantity:
          type: boolean
        allowed_providers:
          type: string
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        cancel_url:
          type: string
          format: uri
          description: URL/URI
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_email:
          type: string
          format: email
          description: Email address
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        customer_name:
          type: string
        customer_phone:
          type: string
        environment:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        installment_plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        is_pos:
          type: boolean
        is_spi:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_link_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        public_description:
          type: string
        qr_code_data:
          type: object
        qr_code_type:
          type: string
        quantity:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_qr_code_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        subscription_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        success_url:
          type: string
          format: uri
          description: URL/URI
        title:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    checkout_sessions_create:
      type: object
      description: Request body for creating a checkout sessions object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        allow_coupon_code:
          type: boolean
        allow_quantity:
          type: boolean
        allowed_providers:
          type: string
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        cancel_url:
          type: string
          format: uri
          description: URL/URI
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_email:
          type: string
          format: email
          description: Email address
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        customer_name:
          type: string
        customer_phone:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        installment_plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        is_pos:
          type: boolean
        is_spi:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        payment_link_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        public_description:
          type: string
        qr_code_data:
          type: object
        qr_code_type:
          type: string
        quantity:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_qr_code_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        subscription_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        success_url:
          type: string
          format: uri
          description: URL/URI
        title:
          type: string
    checkout_sessions_update:
      type: object
      description: Request body for updating a checkout sessions object. Only include fields you want to modify.
      properties:
        allow_coupon_code:
          type: boolean
        allow_quantity:
          type: boolean
        allowed_providers:
          type: string
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        cancel_url:
          type: string
          format: uri
          description: URL/URI
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_email:
          type: string
          format: email
          description: Email address
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        customer_name:
          type: string
        customer_phone:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        installment_plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        is_pos:
          type: boolean
        is_spi:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        payment_link_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        public_description:
          type: string
        qr_code_data:
          type: object
        qr_code_type:
          type: string
        quantity:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_qr_code_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        subscription_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        success_url:
          type: string
          format: uri
          description: URL/URI
        title:
          type: string
    customer_invoices:
      type: object
      description: customer invoices resource object
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        customer_invoice_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        due_date:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    customer_invoices_create:
      type: object
      description: Request body for creating a customer invoices object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        customer_invoice_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        due_date:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        status:
          type: string
          description: Current status of the resource
    customer_invoices_update:
      type: object
      description: Request body for updating a customer invoices object. Only include fields you want to modify.
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        customer_invoice_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        due_date:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        status:
          type: string
          description: Current status of the resource
    customers:
      type: object
      description: customers resource object
      properties:
        address:
          type: string
        city:
          type: string
        country:
          type: string
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        customer_id:
          type: string
          readOnly: true
          format: uuid
          description: Unique identifier (UUID format)
        deleted_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        email:
          type: string
          format: email
          description: Email address
        environment:
          type: string
        is_business:
          type: boolean
        is_deleted:
          type: boolean
          description: Soft deletion flag
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        name:
          type: string
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        phone_number:
          type: string
        postal_code:
          type: string
        spi_alias_mbno:
          type: string
        spi_alias_shid:
          type: string
        spi_primary_alias:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        whatsapp_number:
          type: string
    customers_create:
      type: object
      description: Request body for creating a customers object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        address:
          type: string
        city:
          type: string
        country:
          type: string
        email:
          type: string
          format: email
          description: Email address
        is_business:
          type: boolean
        is_deleted:
          type: boolean
          description: Soft deletion flag
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        name:
          type: string
        phone_number:
          type: string
        postal_code:
          type: string
        spi_alias_mbno:
          type: string
        spi_alias_shid:
          type: string
        spi_primary_alias:
          type: string
        whatsapp_number:
          type: string
    customers_update:
      type: object
      description: Request body for updating a customers object. Only include fields you want to modify.
      properties:
        address:
          type: string
        city:
          type: string
        country:
          type: string
        email:
          type: string
          format: email
          description: Email address
        is_business:
          type: boolean
        is_deleted:
          type: boolean
          description: Soft deletion flag
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        name:
          type: string
        phone_number:
          type: string
        postal_code:
          type: string
        spi_alias_mbno:
          type: string
        spi_alias_shid:
          type: string
        spi_primary_alias:
          type: string
        whatsapp_number:
          type: string
    discount_coupons:
      type: object
      description: discount coupons resource object
      properties:
        applies_to_product_types:
          type: string
        code:
          type: string
        coupon_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        current_uses:
          type: number
          format: double
        customer_type:
          type: string
        description:
          type: string
        discount_fixed_amount:
          type: number
          format: double
        discount_percentage:
          type: number
          format: double
        discount_type:
          type: string
        environment:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_organization_wide:
          type: boolean
        max_quantity_per_use:
          type: number
          format: double
        max_uses:
          type: number
          format: double
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        scope_type:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        usage_frequency_limit:
          type: string
        usage_limit_value:
          type: number
          format: double
        valid_from:
          type: string
    discount_coupons_create:
      type: object
      description: Request body for creating a discount coupons object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        applies_to_product_types:
          type: string
        code:
          type: string
        coupon_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        current_uses:
          type: number
          format: double
        customer_type:
          type: string
        description:
          type: string
        discount_fixed_amount:
          type: number
          format: double
        discount_percentage:
          type: number
          format: double
        discount_type:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_organization_wide:
          type: boolean
        max_quantity_per_use:
          type: number
          format: double
        max_uses:
          type: number
          format: double
        scope_type:
          type: string
        usage_frequency_limit:
          type: string
        usage_limit_value:
          type: number
          format: double
        valid_from:
          type: string
    discount_coupons_update:
      type: object
      description: Request body for updating a discount coupons object. Only include fields you want to modify.
      properties:
        applies_to_product_types:
          type: string
        code:
          type: string
        coupon_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        current_uses:
          type: number
          format: double
        customer_type:
          type: string
        description:
          type: string
        discount_fixed_amount:
          type: number
          format: double
        discount_percentage:
          type: number
          format: double
        discount_type:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_organization_wide:
          type: boolean
        max_quantity_per_use:
          type: number
          format: double
        max_uses:
          type: number
          format: double
        scope_type:
          type: string
        usage_frequency_limit:
          type: string
        usage_limit_value:
          type: number
          format: double
        valid_from:
          type: string
    events:
      type: object
      description: events resource object
      properties:
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        event_data:
          type: object
        event_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        event_name:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    events_create:
      type: object
      description: Request body for creating a events object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        event_data:
          type: object
        event_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        event_name:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    events_update:
      type: object
      description: Request body for updating a events object. Only include fields you want to modify.
      properties:
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        event_data:
          type: object
        event_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        event_name:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    installment_payments:
      type: object
      description: installment payments resource object
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        due_date:
          type: string
        installment_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        interest_amount:
          type: number
          format: double
        paid_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        payment_link:
          type: string
        payment_method_code:
          type: string
        plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        principal_amount:
          type: number
          format: double
        processing_fee:
          type: number
          format: double
        provider_code:
          type: string
        sequence_number:
          type: number
          format: double
        spi_payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        transaction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    installment_payments_create:
      type: object
      description: Request body for creating a installment payments object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        due_date:
          type: string
        installment_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        interest_amount:
          type: number
          format: double
        paid_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        payment_link:
          type: string
        payment_method_code:
          type: string
        plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        principal_amount:
          type: number
          format: double
        processing_fee:
          type: number
          format: double
        provider_code:
          type: string
        sequence_number:
          type: number
          format: double
        spi_payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        transaction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    installment_payments_update:
      type: object
      description: Request body for updating a installment payments object. Only include fields you want to modify.
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        due_date:
          type: string
        installment_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        interest_amount:
          type: number
          format: double
        paid_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        payment_link:
          type: string
        payment_method_code:
          type: string
        plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        principal_amount:
          type: number
          format: double
        processing_fee:
          type: number
          format: double
        provider_code:
          type: string
        sequence_number:
          type: number
          format: double
        spi_payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        transaction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    meter_balances:
      type: object
      description: meter balances resource object
      properties:
        balance:
          type: number
          format: double
        balance_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        billable_organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        consumed_units:
          type: number
          format: double
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        credited_units:
          type: number
          format: double
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        last_event_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        meter_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    meter_balances_create:
      type: object
      description: Request body for creating a meter balances object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        balance:
          type: number
          format: double
        balance_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        billable_organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        consumed_units:
          type: number
          format: double
        credited_units:
          type: number
          format: double
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        last_event_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        meter_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    meter_balances_update:
      type: object
      description: Request body for updating a meter balances object. Only include fields you want to modify.
      properties:
        balance:
          type: number
          format: double
        balance_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        billable_organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        consumed_units:
          type: number
          format: double
        credited_units:
          type: number
          format: double
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        last_event_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        meter_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    meters:
      type: object
      description: meters resource object
      properties:
        aggregation:
          type: object
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        filter:
          type: object
        is_active:
          type: boolean
          description: Whether this resource is currently active
        meter_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        name:
          type: string
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    meters_create:
      type: object
      description: Request body for creating a meters object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        aggregation:
          type: object
        filter:
          type: object
        is_active:
          type: boolean
          description: Whether this resource is currently active
        meter_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        name:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    meters_update:
      type: object
      description: Request body for updating a meters object. Only include fields you want to modify.
      properties:
        aggregation:
          type: object
        filter:
          type: object
        is_active:
          type: boolean
          description: Whether this resource is currently active
        meter_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        name:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    payment_links:
      type: object
      description: payment links resource object
      properties:
        allow_coupon_code:
          type: boolean
        allow_quantity:
          type: boolean
        allowed_providers:
          type: string
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        cancel_url:
          type: string
          format: uri
          description: URL/URI
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        environment:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        link_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        link_type:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        private_description:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        public_description:
          type: string
        quantity:
          type: number
          format: double
        success_url:
          type: string
          format: uri
          description: URL/URI
        title:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        url:
          type: string
          format: uri
          description: URL/URI
    payment_links_create:
      type: object
      description: Request body for creating a payment links object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        allow_coupon_code:
          type: boolean
        allow_quantity:
          type: boolean
        allowed_providers:
          type: string
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        cancel_url:
          type: string
          format: uri
          description: URL/URI
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        link_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        link_type:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        private_description:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        public_description:
          type: string
        quantity:
          type: number
          format: double
        success_url:
          type: string
          format: uri
          description: URL/URI
        title:
          type: string
        url:
          type: string
          format: uri
          description: URL/URI
    payment_links_update:
      type: object
      description: Request body for updating a payment links object. Only include fields you want to modify.
      properties:
        allow_coupon_code:
          type: boolean
        allow_quantity:
          type: boolean
        allowed_providers:
          type: string
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        cancel_url:
          type: string
          format: uri
          description: URL/URI
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        link_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        link_type:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        private_description:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        public_description:
          type: string
        quantity:
          type: number
          format: double
        success_url:
          type: string
          format: uri
          description: URL/URI
        title:
          type: string
        url:
          type: string
          format: uri
          description: URL/URI
    payment_requests:
      type: object
      description: payment requests resource object
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        environment:
          type: string
        expiry_date:
          type: string
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_link:
          type: string
        payment_reference:
          type: string
        request_id:
          type: string
          readOnly: true
          format: uuid
          description: Unique identifier (UUID format)
        spi_account_number:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_confirmation:
          type: boolean
        spi_date_envoi:
          type: string
        spi_date_irrevocabilite:
          type: string
        spi_date_limite_paiement:
          type: string
        spi_date_limite_reponse:
          type: string
        spi_date_rejet:
          type: string
        spi_debit_differe:
          type: boolean
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_payeur_alias:
          type: string
        spi_payeur_nom:
          type: string
        spi_payeur_pays:
          type: string
        spi_payment_request_category:
          type: string
        spi_payment_status:
          type: string
        spi_ref_doc_numero:
          type: string
        spi_ref_doc_type:
          type: string
        spi_rejection_reason:
          type: string
        spi_remise_amount:
          type: number
          format: double
        spi_remise_rate:
          type: number
          format: double
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    payment_requests_create:
      type: object
      description: Request body for creating a payment requests object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        expiry_date:
          type: string
        payment_link:
          type: string
        payment_reference:
          type: string
        spi_account_number:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_confirmation:
          type: boolean
        spi_date_envoi:
          type: string
        spi_date_irrevocabilite:
          type: string
        spi_date_limite_paiement:
          type: string
        spi_date_limite_reponse:
          type: string
        spi_date_rejet:
          type: string
        spi_debit_differe:
          type: boolean
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_payeur_alias:
          type: string
        spi_payeur_nom:
          type: string
        spi_payeur_pays:
          type: string
        spi_payment_request_category:
          type: string
        spi_payment_status:
          type: string
        spi_ref_doc_numero:
          type: string
        spi_ref_doc_type:
          type: string
        spi_rejection_reason:
          type: string
        spi_remise_amount:
          type: number
          format: double
        spi_remise_rate:
          type: number
          format: double
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
    payment_requests_update:
      type: object
      description: Request body for updating a payment requests object. Only include fields you want to modify.
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        expiry_date:
          type: string
        payment_link:
          type: string
        payment_reference:
          type: string
        spi_account_number:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_confirmation:
          type: boolean
        spi_date_envoi:
          type: string
        spi_date_irrevocabilite:
          type: string
        spi_date_limite_paiement:
          type: string
        spi_date_limite_reponse:
          type: string
        spi_date_rejet:
          type: string
        spi_debit_differe:
          type: boolean
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_payeur_alias:
          type: string
        spi_payeur_nom:
          type: string
        spi_payeur_pays:
          type: string
        spi_payment_request_category:
          type: string
        spi_payment_status:
          type: string
        spi_ref_doc_numero:
          type: string
        spi_ref_doc_type:
          type: string
        spi_rejection_reason:
          type: string
        spi_remise_amount:
          type: number
          format: double
        spi_remise_rate:
          type: number
          format: double
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
    payout_methods:
      type: object
      description: payout methods resource object
      properties:
        account_name:
          type: string
        account_number:
          type: string
        auto_withdrawal_day:
          type: number
          format: double
        auto_withdrawal_enabled:
          type: boolean
        auto_withdrawal_last_run:
          type: string
        auto_withdrawal_method:
          type: string
        auto_withdrawal_mobile_provider:
          type: string
        bank_code:
          type: string
        bank_name:
          type: string
        branch_code:
          type: string
        country:
          type: string
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        is_default:
          type: boolean
        is_spi_enabled:
          type: boolean
        is_uemoa:
          type: boolean
        is_valid:
          type: boolean
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_type:
          type: string
        spi_account_number:
          type: string
        spi_alias_mbno:
          type: string
        spi_alias_shid:
          type: string
        spi_alias_type:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    payout_methods_create:
      type: object
      description: Request body for creating a payout methods object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        account_name:
          type: string
        account_number:
          type: string
        auto_withdrawal_day:
          type: number
          format: double
        auto_withdrawal_enabled:
          type: boolean
        auto_withdrawal_last_run:
          type: string
        auto_withdrawal_method:
          type: string
        auto_withdrawal_mobile_provider:
          type: string
        bank_code:
          type: string
        bank_name:
          type: string
        branch_code:
          type: string
        country:
          type: string
        is_default:
          type: boolean
        is_spi_enabled:
          type: boolean
        is_uemoa:
          type: boolean
        is_valid:
          type: boolean
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_type:
          type: string
        spi_account_number:
          type: string
        spi_alias_mbno:
          type: string
        spi_alias_shid:
          type: string
        spi_alias_type:
          type: string
    payout_methods_update:
      type: object
      description: Request body for updating a payout methods object. Only include fields you want to modify.
      properties:
        account_name:
          type: string
        account_number:
          type: string
        auto_withdrawal_day:
          type: number
          format: double
        auto_withdrawal_enabled:
          type: boolean
        auto_withdrawal_last_run:
          type: string
        auto_withdrawal_method:
          type: string
        auto_withdrawal_mobile_provider:
          type: string
        bank_code:
          type: string
        bank_name:
          type: string
        branch_code:
          type: string
        country:
          type: string
        is_default:
          type: boolean
        is_spi_enabled:
          type: boolean
        is_uemoa:
          type: boolean
        is_valid:
          type: boolean
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_type:
          type: string
        spi_account_number:
          type: string
        spi_alias_mbno:
          type: string
        spi_alias_shid:
          type: string
        spi_alias_type:
          type: string
    payouts:
      type: object
      description: payouts resource object
      properties:
        account_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        environment:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_method_code:
          type: string
        payout_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        status:
          type: string
          description: Current status of the resource
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    payouts_create:
      type: object
      description: Request body for creating a payouts object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        account_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        payment_method_code:
          type: string
        payout_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        status:
          type: string
          description: Current status of the resource
    payouts_update:
      type: object
      description: Request body for updating a payouts object. Only include fields you want to modify.
      properties:
        account_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        payment_method_code:
          type: string
        payout_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payout_method_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        status:
          type: string
          description: Current status of the resource
    prices:
      type: object
      description: prices resource object
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        billing_interval:
          type: string
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        environment:
          type: string
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_default:
          type: boolean
        maximum_amount:
          type: number
          format: double
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        minimum_amount:
          type: number
          format: double
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        pricing_model:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    prices_create:
      type: object
      description: Request body for creating a prices object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        billing_interval:
          type: string
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_default:
          type: boolean
        maximum_amount:
          type: number
          format: double
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        minimum_amount:
          type: number
          format: double
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        pricing_model:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    prices_update:
      type: object
      description: Request body for updating a prices object. Only include fields you want to modify.
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        billing_interval:
          type: string
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_default:
          type: boolean
        maximum_amount:
          type: number
          format: double
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        minimum_amount:
          type: number
          format: double
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        pricing_model:
          type: string
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    products:
      type: object
      description: products resource object
      properties:
        billing_frequency:
          type: string
        charge_day:
          type: number
          format: double
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        description:
          type: string
        display_on_storefront:
          type: boolean
        environment:
          type: string
        failed_payment_action:
          type: string
        first_payment_type:
          type: string
        image_url:
          type: string
          format: uri
          description: URL/URI
        is_active:
          type: boolean
          description: Whether this resource is currently active
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        name:
          type: string
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          readOnly: true
          format: uuid
          description: Unique identifier (UUID format)
        product_type:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        usage_aggregation:
          type: string
        usage_unit:
          type: string
    products_create:
      type: object
      description: Request body for creating a products object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        billing_frequency:
          type: string
        charge_day:
          type: number
          format: double
        description:
          type: string
        display_on_storefront:
          type: boolean
        failed_payment_action:
          type: string
        first_payment_type:
          type: string
        image_url:
          type: string
          format: uri
          description: URL/URI
        is_active:
          type: boolean
          description: Whether this resource is currently active
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        name:
          type: string
        product_type:
          type: string
        usage_aggregation:
          type: string
        usage_unit:
          type: string
    products_update:
      type: object
      description: Request body for updating a products object. Only include fields you want to modify.
      properties:
        billing_frequency:
          type: string
        charge_day:
          type: number
          format: double
        description:
          type: string
        display_on_storefront:
          type: boolean
        failed_payment_action:
          type: string
        first_payment_type:
          type: string
        image_url:
          type: string
          format: uri
          description: URL/URI
        is_active:
          type: boolean
          description: Whether this resource is currently active
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        name:
          type: string
        product_type:
          type: string
        usage_aggregation:
          type: string
        usage_unit:
          type: string
    refunds:
      type: object
      description: refunds resource object
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        environment:
          type: string
        fee_amount:
          type: number
          format: double
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        reason:
          type: string
        refund_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        refunded_amount:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_fund_return_status:
          type: string
        spi_motif_code:
          type: string
        spi_rejection_reason:
          type: string
        spi_retour_date_demande:
          type: string
        spi_retour_date_irrevocabilite:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        transaction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    refunds_create:
      type: object
      description: Request body for creating a refunds object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        fee_amount:
          type: number
          format: double
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        reason:
          type: string
        refund_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        refunded_amount:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_fund_return_status:
          type: string
        spi_motif_code:
          type: string
        spi_rejection_reason:
          type: string
        spi_retour_date_demande:
          type: string
        spi_retour_date_irrevocabilite:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        transaction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    refunds_update:
      type: object
      description: Request body for updating a refunds object. Only include fields you want to modify.
      properties:
        amount:
          type: number
          format: double
          description: Amount in the smallest currency unit (e.g., cents for USD, same for XOF)
        fee_amount:
          type: number
          format: double
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        reason:
          type: string
        refund_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        refunded_amount:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_fund_return_status:
          type: string
        spi_motif_code:
          type: string
        spi_rejection_reason:
          type: string
        spi_retour_date_demande:
          type: string
        spi_retour_date_irrevocabilite:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        transaction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    spi_account_aliases:
      type: object
      description: spi account aliases resource object
      properties:
        account_number:
          type: string
        alias_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        alias_key:
          type: string
        alias_type:
          type: string
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_default:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    spi_account_aliases_create:
      type: object
      description: Request body for creating a spi account aliases object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        account_number:
          type: string
        alias_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        alias_key:
          type: string
        alias_type:
          type: string
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_default:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
    spi_account_aliases_update:
      type: object
      description: Request body for updating a spi account aliases object. Only include fields you want to modify.
      properties:
        account_number:
          type: string
        alias_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        alias_key:
          type: string
        alias_type:
          type: string
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_default:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
    spi_qr_codes:
      type: object
      description: spi qr codes resource object
      properties:
        categorie:
          type: string
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        compte_paye:
          type: string
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        environment:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_used:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        montant:
          type: number
          format: double
        name:
          type: string
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payeur_alias:
          type: string
        payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        qr_code_data:
          type: string
        qr_code_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        qr_code_image_data:
          type: string
        qr_code_image_url:
          type: string
          format: uri
          description: URL/URI
        qr_code_type:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    spi_qr_codes_create:
      type: object
      description: Request body for creating a spi qr codes object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        categorie:
          type: string
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        compte_paye:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_used:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        montant:
          type: number
          format: double
        name:
          type: string
        payeur_alias:
          type: string
        payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        qr_code_data:
          type: string
        qr_code_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        qr_code_image_data:
          type: string
        qr_code_image_url:
          type: string
          format: uri
          description: URL/URI
        qr_code_type:
          type: string
    spi_qr_codes_update:
      type: object
      description: Request body for updating a spi qr codes object. Only include fields you want to modify.
      properties:
        categorie:
          type: string
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        compte_paye:
          type: string
        expires_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        is_active:
          type: boolean
          description: Whether this resource is currently active
        is_used:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        montant:
          type: number
          format: double
        name:
          type: string
        payeur_alias:
          type: string
        payment_request_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        plan_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        qr_code_data:
          type: string
        qr_code_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        qr_code_image_data:
          type: string
        qr_code_image_url:
          type: string
          format: uri
          description: URL/URI
        qr_code_type:
          type: string
    subscriptions:
      type: object
      description: subscriptions resource object
      properties:
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        end_date:
          type: string
        environment:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        next_billing_date:
          type: string
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        start_date:
          type: string
        status:
          type: string
          description: Current status of the resource
        subscription_id:
          type: string
          readOnly: true
          format: uuid
          description: Unique identifier (UUID format)
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    subscriptions_create:
      type: object
      description: Request body for creating a subscriptions object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        end_date:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        next_billing_date:
          type: string
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        start_date:
          type: string
        status:
          type: string
          description: Current status of the resource
    subscriptions_update:
      type: object
      description: Request body for updating a subscriptions object. Only include fields you want to modify.
      properties:
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        end_date:
          type: string
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        next_billing_date:
          type: string
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        start_date:
          type: string
        status:
          type: string
          description: Current status of the resource
    transactions:
      type: object
      description: transactions resource object
      properties:
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        discount_amount:
          type: number
          format: double
        environment:
          type: string
        fee_amount:
          type: number
          format: double
        fee_structure_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        gross_amount:
          type: number
          format: double
        is_bnpl:
          type: boolean
        is_pos:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        net_amount:
          type: number
          format: double
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payment_method_code:
          type: string
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        quantity:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_date_envoi:
          type: string
        spi_date_irrevocabilite:
          type: string
        spi_discount_amount:
          type: number
          format: double
        spi_discount_rate:
          type: number
          format: double
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_payment_category:
          type: string
        spi_payment_flow_type:
          type: string
        spi_payment_status:
          type: string
        spi_rejection_reason:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        subscription_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        transaction_id:
          type: string
          readOnly: true
          format: uuid
          description: Unique identifier (UUID format)
        transaction_type:
          type: string
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
    transactions_create:
      type: object
      description: Request body for creating a transactions object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        discount_amount:
          type: number
          format: double
        fee_amount:
          type: number
          format: double
        fee_structure_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        gross_amount:
          type: number
          format: double
        is_bnpl:
          type: boolean
        is_pos:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        net_amount:
          type: number
          format: double
        payment_method_code:
          type: string
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        quantity:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_date_envoi:
          type: string
        spi_date_irrevocabilite:
          type: string
        spi_discount_amount:
          type: number
          format: double
        spi_discount_rate:
          type: number
          format: double
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_payment_category:
          type: string
        spi_payment_flow_type:
          type: string
        spi_payment_status:
          type: string
        spi_rejection_reason:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        subscription_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        transaction_type:
          type: string
    transactions_update:
      type: object
      description: Request body for updating a transactions object. Only include fields you want to modify.
      properties:
        checkout_session_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        currency_code:
          type: string
          description: Three-letter ISO currency code (e.g., XOF, USD, EUR)
        customer_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        description:
          type: string
        discount_amount:
          type: number
          format: double
        fee_amount:
          type: number
          format: double
        fee_structure_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        gross_amount:
          type: number
          format: double
        is_bnpl:
          type: boolean
        is_pos:
          type: boolean
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        net_amount:
          type: number
          format: double
        payment_method_code:
          type: string
        price_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        product_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        provider_code:
          type: string
        quantity:
          type: number
          format: double
        spi_account_number:
          type: string
        spi_bulk_instruction_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_date_envoi:
          type: string
        spi_date_irrevocabilite:
          type: string
        spi_discount_amount:
          type: number
          format: double
        spi_discount_rate:
          type: number
          format: double
        spi_end2end_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        spi_payment_category:
          type: string
        spi_payment_flow_type:
          type: string
        spi_payment_status:
          type: string
        spi_rejection_reason:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        status:
          type: string
          description: Current status of the resource
        subscription_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        transaction_type:
          type: string
    webhook_delivery_logs:
      type: object
      description: webhook delivery logs resource object
      properties:
        attempt_number:
          type: number
          format: double
        compte_paye:
          type: string
        compte_payeur:
          type: string
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        event_type:
          type: string
        headers:
          type: object
        ip_address:
          type: string
        log_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        montant:
          type: number
          format: double
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        payload:
          type: object
        request_duration_ms:
          type: number
          format: double
        response_body:
          type: string
        response_status:
          type: number
          format: double
        spi_event_code:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        success:
          type: boolean
        user_agent:
          type: string
        webhook_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    webhook_delivery_logs_create:
      type: object
      description: Request body for creating a webhook delivery logs object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        attempt_number:
          type: number
          format: double
        compte_paye:
          type: string
        compte_payeur:
          type: string
        event_type:
          type: string
        headers:
          type: object
        ip_address:
          type: string
        log_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        montant:
          type: number
          format: double
        payload:
          type: object
        request_duration_ms:
          type: number
          format: double
        response_body:
          type: string
        response_status:
          type: number
          format: double
        spi_event_code:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        success:
          type: boolean
        user_agent:
          type: string
        webhook_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    webhook_delivery_logs_update:
      type: object
      description: Request body for updating a webhook delivery logs object. Only include fields you want to modify.
      properties:
        attempt_number:
          type: number
          format: double
        compte_paye:
          type: string
        compte_payeur:
          type: string
        event_type:
          type: string
        headers:
          type: object
        ip_address:
          type: string
        log_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        montant:
          type: number
          format: double
        payload:
          type: object
        request_duration_ms:
          type: number
          format: double
        response_body:
          type: string
        response_status:
          type: number
          format: double
        spi_event_code:
          type: string
        spi_tx_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        success:
          type: boolean
        user_agent:
          type: string
        webhook_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    webhooks:
      type: object
      description: webhooks resource object
      properties:
        authorized_events:
          type: string
        created_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        created_by:
          type: string
          readOnly: true
        deleted_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        environment:
          type: string
        is_active:
          type: boolean
          description: Whether this resource is currently active
        last_payload:
          type: object
        last_response_body:
          type: string
        last_response_status:
          type: number
          format: double
        last_triggered_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        organization_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
        retry_count:
          type: number
          format: double
        spi_event_types:
          type: string
        supports_spi:
          type: boolean
        updated_at:
          type: string
          readOnly: true
          format: date-time
          description: ISO 8601 datetime
        url:
          type: string
          format: uri
          description: URL/URI
        verification_token:
          type: string
        webhook_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    webhooks_create:
      type: object
      description: Request body for creating a webhooks object. System-managed fields like `created_at`, `organization_id`, and IDs are automatically set.
      required: []
      properties:
        authorized_events:
          type: string
        is_active:
          type: boolean
          description: Whether this resource is currently active
        last_payload:
          type: object
        last_response_body:
          type: string
        last_response_status:
          type: number
          format: double
        last_triggered_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        retry_count:
          type: number
          format: double
        spi_event_types:
          type: string
        supports_spi:
          type: boolean
        url:
          type: string
          format: uri
          description: URL/URI
        verification_token:
          type: string
        webhook_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    webhooks_update:
      type: object
      description: Request body for updating a webhooks object. Only include fields you want to modify.
      properties:
        authorized_events:
          type: string
        is_active:
          type: boolean
          description: Whether this resource is currently active
        last_payload:
          type: object
        last_response_body:
          type: string
        last_response_status:
          type: number
          format: double
        last_triggered_at:
          type: string
          format: date-time
          description: ISO 8601 datetime
        metadata:
          type: object
          description: Set of key-value pairs for storing additional information
        retry_count:
          type: number
          format: double
        spi_event_types:
          type: string
        supports_spi:
          type: boolean
        url:
          type: string
          format: uri
          description: URL/URI
        verification_token:
          type: string
        webhook_id:
          type: string
          format: uuid
          description: Unique identifier (UUID format)
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: object
    bnpl_status:
      type: string
      enum:
        - pending
        - collected
        - waived
        - refunded
      description: 'Enum: bnpl_status'
    checkout_session_status:
      type: string
      enum:
        - open
        - completed
        - expired
      description: 'Enum: checkout_session_status'
    currency_code:
      type: string
      enum:
        - XOF
        - USD
        - EUR
      description: 'Enum: currency_code'
    frequency:
      type: string
      enum:
        - weekly
        - bi-weekly
        - monthly
        - bi-monthly
        - quarterly
        - semi-annual
        - yearly
      description: 'Enum: frequency'
    invoice_status:
      type: string
      enum:
        - sent
        - paid
        - overdue
        - cancelled
      description: 'Enum: invoice_status'
    payment_method_code:
      type: string
      enum:
        - CARDS
        - MOBILE_MONEY
        - BANK_TRANSFER
        - BNPL
        - FREE
      description: 'Enum: payment_method_code'
    payout_status:
      type: string
      enum:
        - pending
        - processing
        - completed
        - failed
      description: 'Enum: payout_status'
    pricing_model_enum:
      type: string
      enum:
        - standard
        - pay_what_you_want
        - tiered
        - volume
      description: 'Enum: pricing_model_enum'
    product_type_enum:
      type: string
      enum:
        - one_time
        - recurring
        - usage_based
      description: 'Enum: product_type_enum'
    provider_code:
      type: string
      enum:
        - WAVE
        - JUMBO
        - MTN
        - STRIPE
        - SPI
        - CYBERSOURCE
        - FREE
      description: 'Enum: provider_code'
    refund_status:
      type: string
      enum:
        - pending
        - completed
        - failed
      description: 'Enum: refund_status'
    spi_account_status:
      type: string
      enum:
        - OUVERT
        - BLOQUE
        - CLOTURE
      description: 'Enum: spi_account_status'
    spi_account_type:
      type: string
      enum:
        - CACC
        - CARD
        - CASH
        - CHAR
        - CISH
        - CURR
        - DPST
        - SVGS
        - ULAA
      description: 'Enum: spi_account_type'
    spi_payment_status:
      type: string
      enum:
        - INITIE
        - ENVOYE
        - IRREVOCABLE
        - REJETE
      description: 'Enum: spi_payment_status'
    subscription_status:
      type: string
      enum:
        - pending
        - active
        - paused
        - cancelled
        - expired
        - past_due
        - trial
      description: 'Enum: subscription_status'
    transaction_status:
      type: string
      enum:
        - pending
        - completed
        - failed
        - refunded
        - expired
      description: 'Enum: transaction_status'
    webhook_event:
      type: string
      enum:
        - PAYMENT_CREATED
        - PAYMENT_SUCCEEDED
        - PAYMENT_FAILED
        - PAYMENT_CANCELED
        - REFUND_CREATED
        - REFUND_COMPLETED
        - REFUND_FAILED
        - SUBSCRIPTION_CREATED
        - SUBSCRIPTION_RENEWED
        - SUBSCRIPTION_CANCELED
        - CHECKOUT_COMPLETED
        - PROVIDER_STATUS_CHANGED
      description: 'Enum: webhook_event'
  responses:
    BadRequest:
      description: Bad request - Invalid input
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthorized:
      description: Unauthorized - Invalid or missing API key
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Not found - Resource does not exist
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
paths:
  /customers:
    get:
      operationId: listCustomers
      summary: List customers
      description: Customer management - create and manage customer profiles
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/customers'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createCustomer
      summary: Create customer
      description: Customer management - create and manage customer profiles
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/customers_create'
            examples:
              default:
                summary: Sample customer creation
                value:
                  name: Aminata Diallo
                  email: aminata.diallo@example.com
                  phone_number: '+225021234567'
                  metadata:
                    customer_segment: premium
                    referral_source: instagram
      responses:
        '201':
          description: Customer successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customers'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /customers/{customer_id}:
    get:
      operationId: retrieveCustomer
      summary: Retrieve customer
      description: Retrieve a specific customer by its unique identifier.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: customer_id
          in: path
          required: true
          description: Unique identifier for the customer
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Customer retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customers'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateCustomer
      summary: Update customer
      description: Update a specific customer. Only provided fields will be updated.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: customer_id
          in: path
          required: true
          description: Unique identifier for the customer
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/customers_update'
            examples:
              default:
                summary: Sample customer update
                value:
                  name: Aminata Diallo-Kane
                  phone_number: '+225779876543'
                  metadata:
                    customer_segment: vip
                    notes: Upgraded to VIP tier
      responses:
        '200':
          description: Customer successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customers'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteCustomer
      summary: Delete customer
      description: Delete a specific customer. This action cannot be undone.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: customer_id
          in: path
          required: true
          description: Unique identifier for the customer
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Customer successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /payment_requests:
    get:
      operationId: listPaymentRequests
      summary: List payment requests
      description: Payment requests - create payment intents and track status
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payment_requests'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createPaymentRequest
      summary: Create payment request
      description: Payment requests - create payment intents and track status
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/payment_requests_create'
            examples:
              default:
                summary: Sample payment_request creation
                value:
                  amount: 25000
                  currency_code: XOF
                  customer_id: cus_1234567890abcdef
                  description: Payment for premium subscription
                  metadata:
                    order_id: ORD-2024-001
      responses:
        '201':
          description: Payment_request successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payment_requests'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /payment_requests/{request_id}:
    get:
      operationId: retrievePaymentRequest
      summary: Retrieve payment request
      description: Retrieve a specific payment request by its unique identifier.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: request_id
          in: path
          required: true
          description: Unique identifier for the payment request
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Payment_request retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payment_requests'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updatePaymentRequest
      summary: Update payment request
      description: Update a specific payment request. Only provided fields will be updated.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: request_id
          in: path
          required: true
          description: Unique identifier for the payment request
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/payment_requests_update'
            examples:
              default:
                summary: Sample payment_request update
                value:
                  metadata:
                    order_id: ORD-2024-001-UPDATED
                    notes: Customer requested invoice
      responses:
        '200':
          description: Payment_request successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payment_requests'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deletePaymentRequest
      summary: Delete payment request
      description: Delete a specific payment request. This action cannot be undone.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: request_id
          in: path
          required: true
          description: Unique identifier for the payment request
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Payment_request successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /transactions:
    get:
      operationId: listTransactions
      summary: List transactions
      description: Transaction history - view completed and pending transactions
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/transactions'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /transactions/{transaction_id}:
    get:
      operationId: retrieveTransaction
      summary: Retrieve transaction
      description: Retrieve a specific transaction by its unique identifier.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: transaction_id
          in: path
          required: true
          description: Unique identifier for the transaction
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Transaction retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/transactions'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /refunds:
    get:
      operationId: listRefunds
      summary: List refunds
      description: Refund management - process and track refunds
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/refunds'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createRefund
      summary: Create refund
      description: Refund management - process and track refunds
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/refunds_create'
            examples:
              default:
                summary: Sample refund creation
                value:
                  name: Sample refund
                  description: Example refund object
      responses:
        '201':
          description: Refund successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refunds'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /refunds/{refund_id}:
    get:
      operationId: retrieveRefund
      summary: Retrieve refund
      description: Retrieve a specific refund by its unique identifier.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: refund_id
          in: path
          required: true
          description: Unique identifier for the refund
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Refund retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refunds'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateRefund
      summary: Update refund
      description: Update a specific refund. Only provided fields will be updated.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: refund_id
          in: path
          required: true
          description: Unique identifier for the refund
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/refunds_update'
            examples:
              default:
                summary: Sample refund update
                value:
                  metadata:
                    updated_at: '2025-11-21T12:46:27.077Z'
                    updated_reason: Administrative update
      responses:
        '200':
          description: Refund successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refunds'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteRefund
      summary: Delete refund
      description: Delete a specific refund. This action cannot be undone.
      tags:
        - Core
      security:
        - ApiKeyAuth: []
      parameters:
        - name: refund_id
          in: path
          required: true
          description: Unique identifier for the refund
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Refund successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /products:
    get:
      operationId: listProducts
      summary: List products
      description: Product catalog - manage one-time and recurring products
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/products'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createProduct
      summary: Create product
      description: Product catalog - manage one-time and recurring products
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/products_create'
            examples:
              default:
                summary: Sample product creation
                value:
                  name: Premium Subscription
                  description: Monthly premium access with all features
                  product_type: recurring
                  is_active: true
                  metadata:
                    features:
                      - analytics
                      - api_access
                      - priority_support
      responses:
        '201':
          description: Product successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/products'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /products/{product_id}:
    get:
      operationId: retrieveProduct
      summary: Retrieve product
      description: Retrieve a specific product by its unique identifier.
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: product_id
          in: path
          required: true
          description: Unique identifier for the product
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Product retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/products'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateProduct
      summary: Update product
      description: Update a specific product. Only provided fields will be updated.
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: product_id
          in: path
          required: true
          description: Unique identifier for the product
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/products_update'
            examples:
              default:
                summary: Sample product update
                value:
                  name: Premium Plus Subscription
                  description: Enhanced premium access with exclusive features
                  is_active: true
      responses:
        '200':
          description: Product successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/products'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteProduct
      summary: Delete product
      description: Delete a specific product. This action cannot be undone.
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: product_id
          in: path
          required: true
          description: Unique identifier for the product
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Product successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /prices:
    get:
      operationId: listPrices
      summary: List prices
      description: Pricing tiers - manage product pricing
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/prices'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createPrice
      summary: Create price
      description: Pricing tiers - manage product pricing
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/prices_create'
            examples:
              default:
                summary: Sample price creation
                value:
                  product_id: prod_1234567890abcdef
                  amount: 5000
                  currency_code: XOF
                  pricing_model: flat_rate
                  billing_frequency: monthly
                  is_active: true
      responses:
        '201':
          description: Price successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/prices'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /prices/{price_id}:
    get:
      operationId: retrievePrice
      summary: Retrieve price
      description: Retrieve a specific price by its unique identifier.
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: price_id
          in: path
          required: true
          description: Unique identifier for the price
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Price retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/prices'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updatePrice
      summary: Update price
      description: Update a specific price. Only provided fields will be updated.
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: price_id
          in: path
          required: true
          description: Unique identifier for the price
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/prices_update'
            examples:
              default:
                summary: Sample price update
                value:
                  is_active: false
      responses:
        '200':
          description: Price successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/prices'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deletePrice
      summary: Delete price
      description: Delete a specific price. This action cannot be undone.
      tags:
        - Products
      security:
        - ApiKeyAuth: []
      parameters:
        - name: price_id
          in: path
          required: true
          description: Unique identifier for the price
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Price successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /subscriptions:
    get:
      operationId: listSubscriptions
      summary: List subscriptions
      description: Subscription management - create and manage recurring billing
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/subscriptions'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createSubscription
      summary: Create subscription
      description: Subscription management - create and manage recurring billing
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/subscriptions_create'
            examples:
              default:
                summary: Sample subscription creation
                value:
                  customer_id: cus_1234567890abcdef
                  product_id: prod_1234567890abcdef
                  price_id: price_1234567890abcdef
                  billing_frequency: monthly
                  metadata:
                    plan: premium
      responses:
        '201':
          description: Subscription successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/subscriptions'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /subscriptions/{subscription_id}:
    get:
      operationId: retrieveSubscription
      summary: Retrieve subscription
      description: Retrieve a specific subscription by its unique identifier.
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: subscription_id
          in: path
          required: true
          description: Unique identifier for the subscription
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Subscription retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/subscriptions'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateSubscription
      summary: Update subscription
      description: Update a specific subscription. Only provided fields will be updated.
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: subscription_id
          in: path
          required: true
          description: Unique identifier for the subscription
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/subscriptions_update'
            examples:
              default:
                summary: Sample subscription update
                value:
                  metadata:
                    plan: enterprise
                    upgraded_at: '2025-11-21T12:46:27.078Z'
      responses:
        '200':
          description: Subscription successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/subscriptions'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteSubscription
      summary: Delete subscription
      description: Delete a specific subscription. This action cannot be undone.
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: subscription_id
          in: path
          required: true
          description: Unique identifier for the subscription
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Subscription successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /discount_coupons:
    get:
      operationId: listDiscountCoupons
      summary: List discount coupons
      description: Discount coupons - create and manage promotional codes
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/discount_coupons'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createDiscountCoupon
      summary: Create discount coupon
      description: Discount coupons - create and manage promotional codes
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/discount_coupons_create'
            examples:
              default:
                summary: Sample discount_coupon creation
                value:
                  code: WELCOME2024
                  discount_type: percentage
                  discount_percentage: 20
                  max_uses: 100
                  is_active: true
                  expires_at: '2024-12-31T23:59:59Z'
                  description: Welcome discount for new customers
      responses:
        '201':
          description: Discount_coupon successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/discount_coupons'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /discount_coupons/{coupon_id}:
    get:
      operationId: retrieveDiscountCoupon
      summary: Retrieve discount coupon
      description: Retrieve a specific discount coupon by its unique identifier.
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: coupon_id
          in: path
          required: true
          description: Unique identifier for the discount coupon
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Discount_coupon retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/discount_coupons'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateDiscountCoupon
      summary: Update discount coupon
      description: Update a specific discount coupon. Only provided fields will be updated.
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: coupon_id
          in: path
          required: true
          description: Unique identifier for the discount coupon
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/discount_coupons_update'
            examples:
              default:
                summary: Sample discount_coupon update
                value:
                  is_active: false
                  max_uses: 50
      responses:
        '200':
          description: Discount_coupon successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/discount_coupons'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteDiscountCoupon
      summary: Delete discount coupon
      description: Delete a specific discount coupon. This action cannot be undone.
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: coupon_id
          in: path
          required: true
          description: Unique identifier for the discount coupon
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Discount_coupon successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /customer_invoices:
    get:
      operationId: listCustomerInvoices
      summary: List customer invoices
      description: Customer invoices - view subscription invoices
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/customer_invoices'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /customer_invoices/{invoice_id}:
    get:
      operationId: retrieveCustomerInvoice
      summary: Retrieve customer invoice
      description: Retrieve a specific customer invoice by its unique identifier.
      tags:
        - Subscriptions
      security:
        - ApiKeyAuth: []
      parameters:
        - name: invoice_id
          in: path
          required: true
          description: Unique identifier for the customer invoice
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Customer_invoice retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer_invoices'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /checkout_sessions:
    get:
      operationId: listCheckoutSessions
      summary: List checkout sessions
      description: Checkout sessions - create hosted payment pages
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/checkout_sessions'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createCheckoutSession
      summary: Create checkout session
      description: Checkout sessions - create hosted payment pages
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/checkout_sessions_create'
            examples:
              default:
                summary: Sample checkout_session creation
                value:
                  amount: 15000
                  currency_code: XOF
                  product_id: prod_1234567890abcdef
                  success_url: https://example.com/success
                  cancel_url: https://example.com/cancel
                  customer_email: customer@example.com
      responses:
        '201':
          description: Checkout_session successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/checkout_sessions'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /checkout_sessions/{session_id}:
    get:
      operationId: retrieveCheckoutSession
      summary: Retrieve checkout session
      description: Retrieve a specific checkout session by its unique identifier.
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: session_id
          in: path
          required: true
          description: Unique identifier for the checkout session
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Checkout_session retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/checkout_sessions'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateCheckoutSession
      summary: Update checkout session
      description: Update a specific checkout session. Only provided fields will be updated.
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: session_id
          in: path
          required: true
          description: Unique identifier for the checkout session
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/checkout_sessions_update'
            examples:
              default:
                summary: Sample checkout_session update
                value:
                  metadata:
                    updated_at: '2025-11-21T12:46:27.078Z'
                    updated_reason: Administrative update
      responses:
        '200':
          description: Checkout_session successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/checkout_sessions'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteCheckoutSession
      summary: Delete checkout session
      description: Delete a specific checkout session. This action cannot be undone.
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: session_id
          in: path
          required: true
          description: Unique identifier for the checkout session
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Checkout_session successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /payment_links:
    get:
      operationId: listPaymentLinks
      summary: List payment links
      description: Payment links - shareable payment URLs
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payment_links'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createPaymentLink
      summary: Create payment link
      description: Payment links - shareable payment URLs
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/payment_links_create'
            examples:
              default:
                summary: Sample payment_link creation
                value:
                  title: Premium Membership
                  amount: 50000
                  currency_code: XOF
                  description: Annual premium membership
                  product_id: prod_1234567890abcdef
      responses:
        '201':
          description: Payment_link successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payment_links'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /payment_links/{link_id}:
    get:
      operationId: retrievePaymentLink
      summary: Retrieve payment link
      description: Retrieve a specific payment link by its unique identifier.
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: link_id
          in: path
          required: true
          description: Unique identifier for the payment link
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Payment_link retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payment_links'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updatePaymentLink
      summary: Update payment link
      description: Update a specific payment link. Only provided fields will be updated.
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: link_id
          in: path
          required: true
          description: Unique identifier for the payment link
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/payment_links_update'
            examples:
              default:
                summary: Sample payment_link update
                value:
                  metadata:
                    updated_at: '2025-11-21T12:46:27.079Z'
                    updated_reason: Administrative update
      responses:
        '200':
          description: Payment_link successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payment_links'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deletePaymentLink
      summary: Delete payment link
      description: Delete a specific payment link. This action cannot be undone.
      tags:
        - Checkout
      security:
        - ApiKeyAuth: []
      parameters:
        - name: link_id
          in: path
          required: true
          description: Unique identifier for the payment link
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Payment_link successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /payouts:
    get:
      operationId: listPayouts
      summary: List payouts
      description: Payout management - transfer funds to beneficiaries
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payouts'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createPayout
      summary: Create payout
      description: Payout management - transfer funds to beneficiaries
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/payouts_create'
            examples:
              default:
                summary: Sample payout creation
                value:
                  amount: 100000
                  currency_code: XOF
                  beneficiary_account_id: acc_1234567890abcdef
                  description: Monthly payout to vendor
      responses:
        '201':
          description: Payout successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payouts'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /payouts/{payout_id}:
    get:
      operationId: retrievePayout
      summary: Retrieve payout
      description: Retrieve a specific payout by its unique identifier.
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: payout_id
          in: path
          required: true
          description: Unique identifier for the payout
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Payout retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payouts'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updatePayout
      summary: Update payout
      description: Update a specific payout. Only provided fields will be updated.
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: payout_id
          in: path
          required: true
          description: Unique identifier for the payout
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/payouts_update'
            examples:
              default:
                summary: Sample payout update
                value:
                  metadata:
                    updated_at: '2025-11-21T12:46:27.079Z'
                    updated_reason: Administrative update
      responses:
        '200':
          description: Payout successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payouts'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deletePayout
      summary: Delete payout
      description: Delete a specific payout. This action cannot be undone.
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: payout_id
          in: path
          required: true
          description: Unique identifier for the payout
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Payout successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /payout_methods:
    get:
      operationId: listPayoutMethods
      summary: List payout methods
      description: Payout methods - manage beneficiary payout details
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payout_methods'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /payout_methods/{payout_method_id}:
    get:
      operationId: retrievePayoutMethod
      summary: Retrieve payout method
      description: Retrieve a specific payout method by its unique identifier.
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: payout_method_id
          in: path
          required: true
          description: Unique identifier for the payout method
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Payout_method retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payout_methods'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /beneficiary_payouts:
    get:
      operationId: listBeneficiaryPayouts
      summary: List beneficiary payouts
      description: Beneficiary payouts - track individual payout transfers
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/beneficiary_payouts'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createBeneficiaryPayout
      summary: Create beneficiary payout
      description: Beneficiary payouts - track individual payout transfers
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/beneficiary_payouts_create'
            examples:
              default:
                summary: Sample beneficiary_payout creation
                value:
                  name: Sample beneficiary_payout
                  description: Example beneficiary_payout object
      responses:
        '201':
          description: Beneficiary_payout successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/beneficiary_payouts'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /beneficiary_payouts/{payout_id}:
    get:
      operationId: retrieveBeneficiaryPayout
      summary: Retrieve beneficiary payout
      description: Retrieve a specific beneficiary payout by its unique identifier.
      tags:
        - Payouts
      security:
        - ApiKeyAuth: []
      parameters:
        - name: payout_id
          in: path
          required: true
          description: Unique identifier for the beneficiary payout
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Beneficiary_payout retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/beneficiary_payouts'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /installment_payments:
    get:
      operationId: listInstallmentPayments
      summary: List installment payments
      description: BNPL installment payments - track installment payments
      tags:
        - BNPL
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/installment_payments'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /installment_payments/{payment_id}:
    get:
      operationId: retrieveInstallmentPayment
      summary: Retrieve installment payment
      description: Retrieve a specific installment payment by its unique identifier.
      tags:
        - BNPL
      security:
        - ApiKeyAuth: []
      parameters:
        - name: payment_id
          in: path
          required: true
          description: Unique identifier for the installment payment
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Installment_payment retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/installment_payments'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /spi_qr_codes:
    get:
      operationId: listSpiQrCodes
      summary: List spi qr codes
      description: SPI QR codes - generate and manage SPI QR payment codes
      tags:
        - SPI
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/spi_qr_codes'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createSpiQrCode
      summary: Create spi qr code
      description: SPI QR codes - generate and manage SPI QR payment codes
      tags:
        - SPI
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/spi_qr_codes_create'
            examples:
              default:
                summary: Sample spi_qr_code creation
                value:
                  amount: 5000
                  currency_code: XOF
                  description: QR code for in-store payment
                  expires_at: '2024-12-31T23:59:59Z'
      responses:
        '201':
          description: Spi_qr_code successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/spi_qr_codes'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /spi_qr_codes/{qr_code_id}:
    get:
      operationId: retrieveSpiQrCode
      summary: Retrieve spi qr code
      description: Retrieve a specific spi qr code by its unique identifier.
      tags:
        - SPI
      security:
        - ApiKeyAuth: []
      parameters:
        - name: qr_code_id
          in: path
          required: true
          description: Unique identifier for the spi qr code
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Spi_qr_code retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/spi_qr_codes'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateSpiQrCode
      summary: Update spi qr code
      description: Update a specific spi qr code. Only provided fields will be updated.
      tags:
        - SPI
      security:
        - ApiKeyAuth: []
      parameters:
        - name: qr_code_id
          in: path
          required: true
          description: Unique identifier for the spi qr code
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/spi_qr_codes_update'
            examples:
              default:
                summary: Sample spi_qr_code update
                value:
                  metadata:
                    updated_at: '2025-11-21T12:46:27.079Z'
                    updated_reason: Administrative update
      responses:
        '200':
          description: Spi_qr_code successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/spi_qr_codes'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteSpiQrCode
      summary: Delete spi qr code
      description: Delete a specific spi qr code. This action cannot be undone.
      tags:
        - SPI
      security:
        - ApiKeyAuth: []
      parameters:
        - name: qr_code_id
          in: path
          required: true
          description: Unique identifier for the spi qr code
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Spi_qr_code successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /spi_account_aliases:
    get:
      operationId: listSpiAccountAliases
      summary: List spi account aliases
      description: SPI account aliases - manage SPI payment aliases
      tags:
        - SPI
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/spi_account_aliases'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /spi_account_aliases/{alias_id}:
    get:
      operationId: retrieveSpiAccountAliase
      summary: Retrieve spi account aliase
      description: Retrieve a specific spi account aliase by its unique identifier.
      tags:
        - SPI
      security:
        - ApiKeyAuth: []
      parameters:
        - name: alias_id
          in: path
          required: true
          description: Unique identifier for the spi account aliase
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Spi_account_aliase retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/spi_account_aliases'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /webhooks:
    get:
      operationId: listWebhooks
      summary: List webhooks
      description: Webhook configuration - receive real-time event notifications
      tags:
        - Webhooks
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/webhooks'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createWebhook
      summary: Create webhook
      description: Webhook configuration - receive real-time event notifications
      tags:
        - Webhooks
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/webhooks_create'
            examples:
              default:
                summary: Sample webhook creation
                value:
                  url: https://api.example.com/webhooks/lomi
                  events:
                    - payment.succeeded
                    - payment.failed
                  description: Production webhook endpoint
                  is_active: true
      responses:
        '201':
          description: Webhook successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webhooks'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /webhooks/{webhook_id}:
    get:
      operationId: retrieveWebhook
      summary: Retrieve webhook
      description: Retrieve a specific webhook by its unique identifier.
      tags:
        - Webhooks
      security:
        - ApiKeyAuth: []
      parameters:
        - name: webhook_id
          in: path
          required: true
          description: Unique identifier for the webhook
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Webhook retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webhooks'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateWebhook
      summary: Update webhook
      description: Update a specific webhook. Only provided fields will be updated.
      tags:
        - Webhooks
      security:
        - ApiKeyAuth: []
      parameters:
        - name: webhook_id
          in: path
          required: true
          description: Unique identifier for the webhook
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/webhooks_update'
            examples:
              default:
                summary: Sample webhook update
                value:
                  is_active: false
                  description: Temporarily disabled for maintenance
      responses:
        '200':
          description: Webhook successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webhooks'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteWebhook
      summary: Delete webhook
      description: Delete a specific webhook. This action cannot be undone.
      tags:
        - Webhooks
      security:
        - ApiKeyAuth: []
      parameters:
        - name: webhook_id
          in: path
          required: true
          description: Unique identifier for the webhook
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Webhook successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /webhook_delivery_logs:
    get:
      operationId: listWebhookDeliveryLogs
      summary: List webhook delivery logs
      description: Webhook event log - history of webhook deliveries
      tags:
        - Webhooks
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/webhook_delivery_logs'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /webhook_delivery_logs/{log_id}:
    get:
      operationId: retrieveWebhookDeliveryLog
      summary: Retrieve webhook delivery log
      description: Retrieve a specific webhook delivery log by its unique identifier.
      tags:
        - Webhooks
      security:
        - ApiKeyAuth: []
      parameters:
        - name: log_id
          in: path
          required: true
          description: Unique identifier for the webhook delivery log
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Webhook_delivery_log retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webhook_delivery_logs'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /events:
    get:
      operationId: listEvents
      summary: List events
      description: Events - view account activity events
      tags:
        - Events
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/events'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /events/{event_id}:
    get:
      operationId: retrieveEvent
      summary: Retrieve event
      description: Retrieve a specific event by its unique identifier.
      tags:
        - Events
      security:
        - ApiKeyAuth: []
      parameters:
        - name: event_id
          in: path
          required: true
          description: Unique identifier for the event
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Event retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/events'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /meters:
    get:
      operationId: listMeters
      summary: List meters
      description: Usage meters - track usage for usage-based billing
      tags:
        - Usage Billing
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/meters'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      operationId: createMeter
      summary: Create meter
      description: Usage meters - track usage for usage-based billing
      tags:
        - Usage Billing
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/meters_create'
            examples:
              default:
                summary: Sample meter creation
                value:
                  name: Sample meter
                  description: Example meter object
      responses:
        '201':
          description: Meter successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/meters'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /meters/{meter_id}:
    get:
      operationId: retrieveMeter
      summary: Retrieve meter
      description: Retrieve a specific meter by its unique identifier.
      tags:
        - Usage Billing
      security:
        - ApiKeyAuth: []
      parameters:
        - name: meter_id
          in: path
          required: true
          description: Unique identifier for the meter
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Meter retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/meters'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    patch:
      operationId: updateMeter
      summary: Update meter
      description: Update a specific meter. Only provided fields will be updated.
      tags:
        - Usage Billing
      security:
        - ApiKeyAuth: []
      parameters:
        - name: meter_id
          in: path
          required: true
          description: Unique identifier for the meter
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/meters_update'
            examples:
              default:
                summary: Sample meter update
                value:
                  metadata:
                    updated_at: '2025-11-21T12:46:27.080Z'
                    updated_reason: Administrative update
      responses:
        '200':
          description: Meter successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/meters'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      operationId: deleteMeter
      summary: Delete meter
      description: Delete a specific meter. This action cannot be undone.
      tags:
        - Usage Billing
      security:
        - ApiKeyAuth: []
      parameters:
        - name: meter_id
          in: path
          required: true
          description: Unique identifier for the meter
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Meter successfully deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
  /meter_balances:
    get:
      operationId: listMeterBalances
      summary: List meter balances
      description: Meter balances - view current usage balances
      tags:
        - Usage Billing
      security:
        - ApiKeyAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return (1-100)
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: offset
          in: query
          description: Number of items to skip for pagination
          schema:
            type: integer
            default: 0
            minimum: 0
        - name: sort
          in: query
          description: 'Sort order. Format: `field:direction` (e.g., `created_at:desc`)'
          schema:
            type: string
            example: created_at:desc
      responses:
        '200':
          description: Successful response with paginated data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/meter_balances'
                  pagination:
                    type: object
                    properties:
                      limit:
                        type: integer
                        description: Number of items per page
                      offset:
                        type: integer
                        description: Number of items skipped
                      total:
                        type: integer
                        description: Total number of items available
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
  /meter_balances/{balance_id}:
    get:
      operationId: retrieveMeterBalance
      summary: Retrieve meter balance
      description: Retrieve a specific meter balance by its unique identifier.
      tags:
        - Usage Billing
      security:
        - ApiKeyAuth: []
      parameters:
        - name: balance_id
          in: path
          required: true
          description: Unique identifier for the meter balance
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Meter_balance retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/meter_balances'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
tags:
  - name: Core
    description: Customer management - create and manage customer profiles
  - name: Core
    description: Payment requests - create payment intents and track status
  - name: Core
    description: Transaction history - view completed and pending transactions
  - name: Core
    description: Refund management - process and track refunds
  - name: Products
    description: Product catalog - manage one-time and recurring products
  - name: Products
    description: Pricing tiers - manage product pricing
  - name: Subscriptions
    description: Subscription management - create and manage recurring billing
  - name: Subscriptions
    description: Discount coupons - create and manage promotional codes
  - name: Subscriptions
    description: Customer invoices - view subscription invoices
  - name: Checkout
    description: Checkout sessions - create hosted payment pages
  - name: Checkout
    description: Payment links - shareable payment URLs
  - name: Payouts
    description: Payout management - transfer funds to beneficiaries
  - name: Payouts
    description: Payout methods - manage beneficiary payout details
  - name: Payouts
    description: Beneficiary payouts - track individual payout transfers
  - name: BNPL
    description: BNPL installment payments - track installment payments
  - name: SPI
    description: SPI QR codes - generate and manage SPI QR payment codes
  - name: SPI
    description: SPI account aliases - manage SPI payment aliases
  - name: Webhooks
    description: Webhook configuration - receive real-time event notifications
  - name: Webhooks
    description: Webhook event log - history of webhook deliveries
  - name: Events
    description: Events - view account activity events
  - name: Usage Billing
    description: Usage meters - track usage for usage-based billing
  - name: Usage Billing
    description: Meter balances - view current usage balances
